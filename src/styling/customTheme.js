import { createTheme } from '@material-ui/core/styles';

export const customTheme = createTheme({
  palette: {
    primary: {
        main: '#e54968',
    },
    secondary: {
        main: '#f44336',
    },
  },
});